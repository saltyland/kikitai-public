# 引継ぎ（キキタイ開発）

> このファイルはチャット（セッション）をまたいで文脈を引き継ぐためのものです。
> **作業を始める前に必ず読み**、**状況が変わったら必ず更新**してください。
> 最終更新：2026-06-10（条件付き表示=分岐ロジックを追加／設問タイプの画像ピッカー・結果グラフ）

## ★直近の変更（条件付き表示＝分岐）
- 設問に「表示条件」を追加。先行の選択式設問で特定の選択肢が選ばれた人だけに、その設問を表示。
- DB：`questions.condition jsonb`（migration `20260610150000_question_condition.sql`、sync済み）。
  形式 `{ sourceQuestionOrder:number, optionText:string }`。null は常に表示。
- 共通ロジック：`lib/domain/questions/visibility.ts` の `computeVisibleQuestionIds()` を
  クライアント(`AnswerForm`)とサーバー(`responseService.submitResponse`)の両方で使用し、
  表示・必須検証・保存対象を一致させる。**非表示設問は必須でも検証されず保存もされない**。
- 編集UI：`SurveyEditor` の `ConditionEditor`。条件元は「自分より前の選択式設問」のみ。
  editor内では source を `key` で参照し、保存時に `order_index` へ変換（`fromSurvey`で復元）。

## ★直近の変更（UI直感化）
- `components/QuestionTypePicker.tsx` を新設。作成画面の設問タイプ選択を、固有名詞だけの
  `<select>` から「プルタブ→開くと各タイプの完成イメージ図（SVG）ボタンが並ぶ」UIに置換
  （`SurveyEditor.tsx` で使用）。やさしい呼び名＋固有名詞を補足表示。
- `components/ResultChart.tsx` を新設。結果ページの選択式集計を横バーのみから
  「円グラフ（ドーナツ）＋凡例＋カラー棒グラフ」に変更（`results/page.tsx`）。
  複数選択は割合合計が100%にならないため棒グラフのみ・total=回答数で算出。
- 注意：eslintの `react-hooks/immutability` でレンダー中の変数再代入は不可。累積値は
  `slice().reduce()` で関数的に計算すること。

---

## 0. これは何のプロジェクト？

学生・研究者が互いにアンケートに回答し合うP2P型学術アンケート交換プラットフォーム
**「キキタイ」** のWebアプリ。現在は **Phase 1（デモ）** を実装済み。

- 作業フォルダ：`C:\大学\キキタイ\キキタイ\kikitai`
- 要件定義：親フォルダの `要件定義_v1.md`（デモ範囲＝正）／ `要件定義.md`（全体v2.0・将来機能）
- 開発ロードマップ：親フォルダの `開発ロードマップ.md`

## 1. 技術スタック

- Next.js **16**（App Router / TypeScript / Turbopack）※従来のNextと破壊的変更あり（後述）
- Tailwind CSS v4
- Supabase（PostgreSQL + Auth）
- Supabase CLI（`supabase` をdev依存に同梱、`npx supabase` で利用）

## 2. 環境・実行の前提（重要な落とし穴）

- **Node.js は PATH に通っていない**。実行時は毎回 PATH 追加が必要：
  - PowerShell: `$env:Path = "C:\Program Files\nodejs;" + $env:Path`
  - node: v24 / npm: v11
- **`.env.local` はUTF-8（BOMなし）**で保存すること。BOMが付くと先頭キーが壊れる。
- 開発サーバー：`npm run dev` → http://localhost:3000
- 型チェック：`npx tsc --noEmit` / ビルド：`npx next build`

### Next.js 16 の注意（AGENTS.md も参照）
- `cookies()` / `params` / `searchParams` は **非同期（Promise）**。`await` する。
- middleware は **`proxy.ts`** に改称（ルート直下）。本アプリの認証ガードは `proxy.ts` + `lib/supabase/proxy.ts`。
- コードを書く前に `node_modules/next/dist/docs/` の該当ガイドを読む方針。

## 3. アーキテクチャ（OOP）

UIから直接DBアクセスしない。層を分離：

```
app/                ページ＋ app/actions/（サーバーアクション）
components/          UI（クライアント/サーバー）
lib/
  supabase/         client(browser) / server / admin(service_role) / proxy
  services/         ビジネスロジック（AuthService 等）
  repositories/     DBアクセス（BaseRepository<T> を継承）
  domain/questions/ 設問タイプの多態（後述）
  types/
proxy.ts            認証ガード
supabase/migrations/  DBスキーマ（冪等マイグレーション）
scripts/supabase-sync.mjs  npm run sync の実体
```

- **設問タイプの多態**：`QuestionTypeDefinition`（抽象親）→ `ChoiceQuestionBase` → Single/Multiple/Scale、`TextQuestion`。`QuestionTypeRegistry` 経由でのみ利用。**新タイプ（順位付け・マトリクス等）はクラス追加＋レジストリ登録だけ**で済む設計（将来のポイント `pointCost` も保持）。
- **リポジトリ抽象基底**：`BaseRepository<T>`（共通CRUD）。

## 4. Supabase 運用

- スキーマは `supabase/migrations/` でバージョン管理（**冪等**＝再実行安全）。
- **手動でSQLをダッシュボードに貼らない**。`npm run sync` か `npm run db:push` で反映。
- `npm run sync` の中身（`scripts/supabase-sync.mjs`）：
  1. Management API で **メール確認OFF（mailer_autoconfirm=true）** を設定
  2. マイグレーション反映：**DBパスワードがあれば `supabase db push`**、
     **無ければ Management API の query エンドポイントでマイグレーションSQLを直接実行**
     （`SUPABASE_ACCESS_TOKEN` だけで反映可能。冪等前提のフォールバック）。
     → これにより `SUPABASE_DB_PASSWORD` 未設定でもスキーマ反映が自動化された。
- 必要な秘密情報（`.env.local`・git管理外）：
  - `SUPABASE_ACCESS_TOKEN`（https://supabase.com/dashboard/account/tokens）
  - `SUPABASE_DB_PASSWORD`（db push に必要。未設定だとpushはスキップ）
  - `SUPABASE_SERVICE_ROLE_KEY`（退会でauth.usersごと削除する場合）
- プロジェクト参照（ref）：`cpyhkdlbwsiiwwucpkji`

### メール確認は OFF 済み
新規登録の `Invalid login credentials` 問題は **メール確認をOFFにして解決済み**
（ダッシュボード＋ `npm run sync` の両方で適用）。登録すると即ログイン状態でホームに入る。
※ OFF前に作った「未確認アカウント」は Authentication → Users から削除して登録し直す。

## 5. Git / PR 状況

- リモート：`https://github.com/TakatoGokyu/softwareA-prac`（default: `main`、自分のWRITE権限あり）
- 作業ブランチ：`feature/phase1-demo`
- **PR #1**：Phase 1デモ → **マージ済み**
- **PR #2**：メール制限解消・CLI自動化・レビュー指摘修正 → **オープン中**
  - https://github.com/TakatoGokyu/softwareA-prac/pull/2
- **PR #3**（オープン中）：https://github.com/TakatoGokyu/softwareA-prac/pull/3
  作業ブランチ `feature/google-form-style-surveys`。
  アンケート作成・閲覧・集計を **Googleフォーム相当** に再現＋改良。
  - 設問タイプ追加：プルダウン / 段落（長文）/ 日付 / グリッド（選択式・チェックボックス）、
    スケールを **可変（min〜max＋両端ラベル）** に拡張。`text` は記述式（短文）に。
  - 設問の **必須(required)トグル**・**設問ごとの説明文**。
  - **セクション（ページ分割）＋進捗バー**。回答画面はページ送り＋ページ内必須バリデーション。
  - 作成画面：**ドラッグ&ドロップ並べ替え**・**設問複製**・セクション間移動。
  - 結果：グリッドのクロス集計表表示＋**CSVダウンロード**（`/surveys/[id]/results/export`）。
  - 設計：設問タイプの多態（QuestionTypeDefinition系）に新クラスを追加しレジストリ登録のみ。
    必須判定・config(scale/grid)・CSV整形(renderAnswerText)も各タイプ定義にカプセル化。
  - **要DB反映**：マイグレーション `supabase/migrations/20260610130000_google_form_features.sql`
    （questions に required/description/config/section_index、surveys に sections、answers に
    row_label を追加。冪等）。**`npm run sync`（または `npm run db:push`）で反映が必要**。
- コミット規約：末尾に `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`
- `.env.local` は絶対にコミットしない（gitignore済み）。

### チーム共有のされ方（CLAUDE.md / SOP / 秘密情報）

- **Git管理下のものは共有される**：`CLAUDE.md`・`AGENTS.md`・`引継ぎ.md`・
  `scripts/supabase-sync.mjs`・`supabase/migrations/` 等。Claude Code は作業フォルダの
  `CLAUDE.md` を自動で読むため、**相手がそのコミットを pull すれば相手側のClaudeも同じSOPに従う**。
  - ただし反映は **PRがmainにマージされ、相手が `git pull` した後**（または当該ブランチを
    `fetch`/`checkout` した後）。未マージのブランチ上の変更は相手にはまだ効かない。
- **共有されないもの（各自で用意が必要）**：
  - `.env.local`（`SUPABASE_ACCESS_TOKEN` 等の秘密情報）は gitignore 済み。
    相手が `npm run sync` でDB反映を行うには **相手自身のアクセストークンを各自の `.env.local`
    に設定**する必要がある（未設定だと sync はDB反映をスキップ）。
  - 個人設定（`~/.claude` のメモリ・キーバインド等）もリポジトリ外なので共有されない。
- **DBは共有の1つ**：スキーマ反映は誰か1人が `npm run sync` すれば全員に反映される。
  相手が sync を叩くのは「自分で新しいマイグレーションを追加したとき」だけでよい。

## 6. レビュー（修正ポイント）

`docs/demo_review_fix_points.md` にサブエージェント（Sonnet 4.6）のレビューあり。
- **高優先度6件＋軽微2件（FIX-01〜06,11,15）は対応済み**。
- 中・低（FIX-07〜10,12〜14,16）は**未対応・記録のみ**（必要に応じ着手）。

## 7. 未対応・TODO / 申し送り

- [x] ✅ **PR3のDB反映済み**：`npm run sync` で `20260610130000_google_form_features.sql`
      をリモートDBへ適用済み（Management API 経由。新カラム6件をライブDBで確認済み）。
- [ ] 🔴 **アクセストークンの再発行**：チャットに一度貼られたトークン（`sbp_2ac6...`）は漏洩扱い。Revoke して再発行し `.env.local` を差し替える（ユーザー作業）。
- [x] `SUPABASE_DB_PASSWORD` 未設定問題は解消（sync が Management API でSQLを直接反映する
      フォールバックを実装済み）。DBパスワードを設定すれば従来どおり `db push` も使える。
- [ ] レビュー中・低優先度（FIX-07〜10,12〜14,16）の対応可否を判断。
- [ ] 退会でauth.users削除を有効にするなら `SUPABASE_SERVICE_ROLE_KEY` を設定。

## 8. このファイルの運用ルール

- セッション開始時に**必ず読む**（CLAUDE.md から参照される）。
- 次のような変化があったら**その都度このファイルを更新**する：
  - 機能・設計の追加/変更、PRの作成・マージ、ブランチ変更
  - Supabase設定・環境変数・運用手順の変更
  - 未対応TODOの増減
- 更新時は冒頭の「最終更新」日付も直す。
