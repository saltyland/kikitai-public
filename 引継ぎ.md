# 引継ぎ（キキタイ開発）

> このファイルはチャット（セッション）をまたいで文脈を引き継ぐためのものです。
> **作業を始める前に必ず読み**、**状況が変わったら必ず更新**してください。
> 古くなった経緯の説明はためこまず、**「直前にやったこと」＋「次にやるかも」だけ**を残すこと. **「直前にやったこと」**は基本的に最新三つまで保持してそれ以外は適宜消すこと
> 最終更新：2026-06-17（条件分岐の複数選択対応）



---

## 0. これは何のプロジェクト？

**「キキタイ」**＝学生・研究者のための **P2P型の学術アンケート交換プラットフォーム**（Webアプリ）。

### 解こうとしている課題
研究や授業課題でアンケートを作っても「回答者が集まらない」。逆に、回答する側にはメリットがない。
そこで **“回答し合う”経済圏** を作る：

1. 他人のアンケートに**回答するとポイントが貯まる**。
2. 貯めたポイントを使って、**自分のアンケートに回答者を集める**（＝公開コストを払う想定）。
3. 回答の質を **AIが自動評価**し、雑な回答は報酬倍率0／信頼スコア減点。良質な回答は×1.5。
   → 「とりあえず埋めるだけ」の回答を抑え、データの質を担保する。
4. プロフィール属性は**非公開にするほどポイントボーナス**がもらえる逆インセンティブ設計
   （非公開にした属性はマッチング＝ターゲティングに使われない代わりにボーナス）。

### 主要な機能（実装済み）
- **アカウント**：Supabase Auth でメール＋パスワード登録／ログイン（メール確認はOFF運用）。
- **アンケート作成**：Googleフォーム相当。設問タイプ＝単一/複数選択・プルダウン・記述/段落・日付・
  スケール（可変段階＋両端ラベル）・グリッド。必須トグル・説明文・**セクション（ページ分割）**・
  **表示条件（分岐）**・ドラッグ&ドロップ並べ替え・複製・テンプレートライブラリ・左右2ペインの即時プレビュー。
- **回答**：1問ずつのステッパー形式。進捗表示・途中保存（localStorage）・送信前確認・スマホ最適化・
  オフライン時の再送（Service Worker）。
- **集計／結果**：作成者のみ閲覧。円グラフ＋棒グラフ、グリッドのクロス集計、CSVダウンロード。
  Proプラン限定の**統計解析モード**（平均・中央値・標準偏差・最頻値）。
- **AI品質評価＋ポイント**：上記のとおり（直前にやったこと §2/§3）。
- **料金プラン**：free / pro（デモは決済なし、トグルで切替）。

### 立ち位置（フェーズ）
現在は **Phase 1（デモ）** ＝コア体験を一通り通す段階。決済・本格的なマッチング配信・通知などは将来フェーズ。

### 参照ドキュメント（親フォルダ `C:\大学\キキタイ\キキタイ`）
- 要件定義：`要件定義_v1.md`（デモ範囲＝正） / `要件定義.md`（全体v2.0・将来機能）
- 開発ロードマップ：`開発ロードマップ.md`
- デザイン仕様：`C:\大学\fable\DESIGN_SPEC.md`
- 作業フォルダ：`C:\大学\キキタイ\キキタイ\kikitai`（Next.jsプロジェクト直下）

## 1. 技術スタック

- Next.js **16**（App Router / TypeScript / Turbopack）※従来のNextと破壊的変更あり（後述）
- Tailwind CSS v4（`@import "tailwindcss"` / `@theme`。`tailwind.config.ts` は使わない）
- Supabase（PostgreSQL + Auth）
- Supabase CLI（`supabase` をdev依存に同梱、`npx supabase` で利用）
- zod（サーバーアクションの payload 実行時検証）

## 2. 環境・実行の前提（重要な落とし穴）

- **Node.js は PATH に通っていない**。実行時は毎回 PATH 追加が必要：
  - PowerShell: `$env:Path = "C:\Program Files\nodejs;" + $env:Path`
  - node: v24 / npm: v11
- **`.env.local` はUTF-8（BOMなし）**で保存すること。BOMが付くと先頭キーが壊れる。
- 開発サーバー：`npm run dev` → http://localhost:3000
- 型チェック：`npx tsc --noEmit` / ビルド：`npx next build`

### Next.js 16 の注意（AGENTS.md も参照）
- `cookies()` / `params` / `searchParams` は **非同期（Promise）**。`await` する。
- middleware は **`proxy.ts`** に改称（ルート直下）。本アプリの認証ガードは `proxy.ts` + `lib/supabase/proxy.ts`。
- コードを書く前に `node_modules/next/dist/docs/` の該当ガイドを読む方針。

## 3. アーキテクチャ（OOP）

UIから直接DBアクセスしない。層を分離：

```
app/                ページ＋ app/actions/（サーバーアクション）
components/          UI（クライアント/サーバー）。ui/ は共通デザイン部品（Button/Card）
lib/
  supabase/         client(browser) / server / admin(service_role) / proxy
  services/         ビジネスロジック（AuthService / ResponseService / ProfileService 等）
  repositories/     DBアクセス（BaseRepository<T> を継承）
  domain/questions/ 設問タイプの多態（後述）
  domain/quality/   AI品質評価（IQualityEvaluator / Gemini / ルールベース）
  utils.ts          cn（クラス名結合）
  types/
proxy.ts            認証ガード
supabase/migrations/  DBスキーマ（冪等マイグレーション）
scripts/supabase-sync.mjs  npm run sync の実体
```

- **設問タイプの多態**：`QuestionTypeDefinition`（抽象親）→ Choice系 / Text / Grid 等。`QuestionTypeRegistry`
  経由でのみ利用。**新タイプはクラス追加＋レジストリ登録だけ**で済む設計（`pointCost` も各タイプが保持）。
- **品質評価の多態（Strategy）**：`IQualityEvaluator` を `createQualityEvaluator()` で切替。
- **リポジトリ抽象基底**：`BaseRepository<T>`（共通CRUD）。

## 4. Supabase 運用

- スキーマは `supabase/migrations/` でバージョン管理（**冪等**＝再実行安全。`add column if not exists` 等）。
- **手動でSQLをダッシュボードに貼らない**。`npm run sync` か `npm run db:push` で反映。
- `npm run sync`（`scripts/supabase-sync.mjs`）：
  1. Management API で **メール確認OFF（mailer_autoconfirm=true）** を設定
  2. マイグレーション反映：**DBパスワードがあれば `supabase db push`**、無ければ Management API の
     query エンドポイントでSQLを直接実行（`SUPABASE_ACCESS_TOKEN` だけで反映可能）。
- 必要な秘密情報（`.env.local`・git管理外）：
  - `SUPABASE_ACCESS_TOKEN`（https://supabase.com/dashboard/account/tokens）
  - `SUPABASE_DB_PASSWORD`（db push に必要。未設定だとpushはスキップ）
  - `SUPABASE_SERVICE_ROLE_KEY`（退会でauth.usersごと削除する場合）
  - `GEMINI_API_KEY`（任意。あれば AI評価が Gemini 動作。無ければルールベース）
- プロジェクト参照（ref）：`cpyhkdlbwsiiwwucpkji`
- メール確認はOFF済み（登録すると即ログイン）。OFF前の未確認アカウントは Authentication → Users から削除。

## 5. Git / PR / Vercel

- リモート：`https://github.com/TakatoGokyu/softwareA-prac`（default: `main`、WRITE権限あり）
- 現在の作業ブランチ：**`feature/design-spec-reflect`**（DESIGN_SPEC 反映）→ **PR #17**（オープン中）
  - https://github.com/TakatoGokyu/softwareA-prac/pull/17
- 本番：**https://kikitai.vercel.app**（main push で自動デプロイ、PRで Preview）。
  環境変数 `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` は Production/Development に設定済み
  （**Preview 環境は未設定**。必要ならダッシュボードから手動追加）。
- コミット規約：末尾に `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。本文は日本語で簡潔に。
- **`.env.local` は絶対にコミットしない**（gitignore済み）。
- チーム共有：Git管理下（`CLAUDE.md`/`AGENTS.md`/`引継ぎ.md`/`migrations` 等）は pull で共有される。
  `.env.local` の秘密情報は各自で用意。DBスキーマは誰か1人が `npm run sync` すれば全員に反映。

## 6. 未対応・TODO / 申し送り

- [ ] 🔴 **アクセストークンの再発行**：過去チャットに貼られたトークン（`sbp_2ac6...`）は漏洩扱い。
      Revoke して再発行し `.env.local` を差し替える（ユーザー作業）。
- [ ] PR #17 のレビュー対応・main へのマージ。
- [ ] 退会で auth.users 削除を有効にするなら `SUPABASE_SERVICE_ROLE_KEY` を設定。
- [ ] レビュー記録 `docs/demo_review_fix_points.md` の中・低優先度（FIX-07〜10,12〜14,16）の対応可否判断。

## 7. このファイルの運用ルール

- セッション開始時に**必ず読む**（CLAUDE.md から参照される）。
- 機能・設計の変更、PR作成/マージ、ブランチ変更、Supabase/環境変数/運用の変更、TODOの増減があったら更新。
- **更新時は「★直前にやったこと」を最新の1件に置き換え、古い経緯は消す**（ためこまない）。冒頭の日付も直す。
