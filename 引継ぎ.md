# 引継ぎ（キキタイ開発）

> このファイルはチャット（セッション）をまたいで文脈を引き継ぐためのものです。
> **作業を始める前に必ず読み**、**状況が変わったら必ず更新**してください。
> 最終更新：2026-06-11（design_export を全面採用：brandパレット＋Button/Cardコンポーネント化）

## ★直近の変更（design_export デザイン採用 / ブランチ `feature/design-spec-reflect`）
親フォルダ `C:\大学\fable\design_export.zip`（より洗練されたデザイン書き出し）を全面採用。
前回の自前sky実装から差し替え。**Tailwind v4 に適合**させて移植。
- **brand カラースケール**を `app/globals.css` の `@theme` に定義（#338eff〜系の青）。アクセントを
  sky → **brand** へ統一（Header/ホーム/一覧/認証/プロフィール）。背景はブロブ3枚（`layout.tsx` の
  固定divレイヤー）＋ `sky-100→white→brand-100` グラデ。
- **立体ユーティリティを差し替え**：`.card-3d`（青み多層シャドウ＋背景/角丸/境界を内包）/
  `.card-3d-hover` / `.btn-3d`（inset白＋下厚み＋落ち影、active沈み込み）/ `.glass`。
  クラス用バリアント `.btn-3d-primary/-secondary/-danger/-ghost` も export のグラデ質感に更新。
- **コンポーネント追加**：`components/ui/button.tsx`（variant: default/secondary/outline/ghost/destructive、
  size: sm/md/lg/icon）・`components/ui/card.tsx`（Card/CardHeader/CardTitle/CardDescription/CardContent）。
  `lib/utils.ts` に軽量 `cn`（clsx/tailwind-merge は入れず最小実装）。
- 検証：`tsc` / `lint` / `next build` 通過。`npm run dev`（/login 200）。DB変更なし。
  ※ アンケート作成/回答系の既存コンポーネント（SurveyEditor/AnswerForm 等）は従来パレットのまま
    （§4の「作成画面は構造踏襲」方針。必要なら別途 brand 化）。


## ★直近の変更（DESIGN_SPEC §2/§3 実装 / ブランチ `feature/design-spec-reflect`）
DESIGN_SPEC §2（AI評価）・§3（プロフィール属性／ポイント／非公開ボーナス）を実装。**要DB反映済み**
（`20260611100000_profile_attributes_points.sql` を `npm run sync` でリモート反映済み）。
- **§2 AI評価システム**：`lib/domain/quality/`（`types.ts`/`ruleBased.ts`/`gemini.ts`/`index.ts`）。
  `IQualityEvaluator` を Strategy で切替（`createQualityEvaluator()`）。`GEMINI_API_KEY` があれば
  Gemini(`gemini-1.5-flash`)、無ければ／失敗時はルールベースへ自動フォールバック。
  ルール：アテンションチェック誤答→0点、短文(<10字)→-20、スケール一直線→-15。
  `responseService.submitResponse` が保存後に評価→倍率(≥80:×1.5/50-79:×1.0/<50:×0)で
  `報酬pt=round(設問pointCost合計×倍率)` を回答者へ付与。スコア0なら `trust_score -=5`。
  結果はホームの `?answered=1&score=&pts=` で表示。
- **§3 プロフィール拡張**：`profiles` に age/gender/occupation/grade/major/points/trust_score(0-100,既定70)/
  private_fields(jsonb) を追加。`point_lots` テーブル新設（有効期限付きポイント束、available=期限内合計）。
  新規登録トリガーで +100pt(180日) を付与。プロフィール画面で各属性に「非公開にする」チェック→
  非公開1項目ごと +10pt(上限50pt)を `recomputePrivacyBonus` で冪等に再計算。
  残り14日以内に失効する束を警告表示。`PointLotRepository`/`ProfileService` 拡張。
- 検証：`tsc` / `lint` / `next build` / `npm run sync` すべて通過。`npm run dev` で起動確認（login 200）。

## ★直近の変更（デザインシステム刷新 / ブランチ `feature/design-spec-reflect`）
親フォルダ `C:\大学\fable\DESIGN_SPEC.md` の「良い」とされたデザイン仕様を反映。**DB変更なし・全てUI**。
- **フォント**：Geist → **M PLUS Rounded 1c**（weights 400/500/700/800、`--font-rounded`）に変更（`app/layout.tsx`）。
- **`app/globals.css` をデザイントークン化**：sky系カラーパレット（CSS変数）、135deg空色グラデ背景＋
  `body::before/::after` の浮遊ブロブ（blur 80px）。ユーティリティ追加：
  - `.card-3d` / `.card-3d-hover`（多層シャドウの奥行きカード）
  - `.btn-3d` ＋ `-primary/-secondary/-danger/-ghost`（`box-shadow: 0 4px 0 edge` + `:active`で translateY する物理的押し込み感）
  - `.glass`（backdrop-filter: blur のすりガラス）
  - reduced-motion 対応済み。
- **適用画面**：Header（glass + sky + sticky）、ホーム（`page.tsx`：ヒーローコピー「こたえて、あつめる。研究の輪。」
  ＋3Dカード/ボタン）、surveys一覧、login/register（card-3d）、ProfileForm（card-3d + btn-3d）、
  DeleteSurveyButton。indigo/zinc → sky/slate へ統一。
- 検証：`npx tsc --noEmit` / `npm run lint` / `npx next build` すべて通過。
- ※ §2/§3 は後続コミットで実装済み（上記参照）。


## ★直近の変更（Vercelデプロイ＋セキュリティ修正 Phase A）

### Vercel 本番デプロイ環境（構築済み）
- 本番URL：**https://kikitai.vercel.app**（Vercelプロジェクト `kikitai` / GitHub連携済み）
- **main に push すると自動で本番デプロイ**、PRを作るとPreviewデプロイが走る。
- 環境変数（`NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY`）は
  Production / Development に設定済み。**Preview 環境は未設定**（CLI不具合のため、
  Vercelダッシュボード → kikitai → Settings → Environment Variables から手動追加が必要）。

### セキュリティ・正確性の修正（ブランチ `fix/security-correctness`）
2体のサブエージェント（シニアエンジニア/UIUX）による全コードレビューを実施し、
重大度の高い指摘から段階的に修正中。本ブランチは第1弾（Phase A）：
- **zod導入**：`lib/domain/schemas.ts` 新設。サーバーアクションの payload を
  `JSON.parse`+`as` ではなく zod で実行時検証（`saveSurveyAction` / `submitResponseAction`）。
- **N+1クエリ解消**：`listMySurveys` / `listAnswerableSurveys` を一括クエリ化
  （`countResponsesBySurveyIds` / `findRespondedSurveyIds` / `findByIds` を各リポジトリに追加）。
- **退会の安全化**：`SUPABASE_SERVICE_ROLE_KEY` 未設定時は profiles だけ消す
  フォールバックをやめ、エラー表示（Authユーザー残存による再登録不可を防止）。
- **DBエラーのサニタイズ**：`lib/repositories/dbError.ts` 新設。Supabaseの生エラーは
  サーバーログのみに出し、ユーザーには一般化メッセージ。重複回答（unique違反）は
  「すでに回答済みです」に変換。
- **パスワードポリシー**：「小文字数字のみ6文字以上」→「8文字以上・英小文字と数字を含む
  （記号・大文字も可）」（register側のみ。既存ユーザーのログインに影響なし）。
- **回答改ざんの防御**：`submitResponse` で「他設問/他アンケートの option_id 混入」
  「同一設問への重複回答」を弾く検証を追加。
- **CSV行順序の安定化**：`findAnswersBySurvey` に明示的な order を追加。

レビュー全体の修正計画（Phase A〜E：a11y/モバイル/issues対応/テスト・CI）は段階的にPR予定。

## ★直近の変更（UX強化 8機能）
作成画面（`SurveyEditor`）と回答画面（`AnswerForm`）に以下を追加。型チェック・lint・`next build` 通過済み。**DB変更なし**（全てクライアント側）。
- **① 回答者プレビュー即時表示**：編集画面を左右2ペイン化（編集ページを `max-w-6xl` に拡張）。右ペインに
  `components/SurveyPreview.tsx` が編集中データをそのまま描画し、PC/スマホのフレーム切替・条件分岐の挙動も試せる。
  「プレビューを隠す」トグル、右ペインは「👁プレビュー / 🔀分岐フロー」タブ切替。
- **② 設問単位のバリデーション警告**：`lib/domain/validation.ts`（純粋関数 `validateEditorQuestion`）。
  各設問カードに警告バッジ＋インライン一覧。公開ボタン押下時に **error が残っていればモーダルで未解決一覧**を出し中断。
  warn は注意のみ（公開は妨げない）。
- **③ 設問テンプレートライブラリ**：`lib/domain/questionTemplates.ts`（デモグラ/リッカート/心理尺度/自由記述/NPS等）＋
  `components/QuestionTemplates.tsx`（モーダル）。ワンクリックで指定セクション末尾に挿入。**マイテンプレート**は
  localStorage `kikitai-my-templates` に保存/再利用/削除。ツールバーと各セクション末尾に「📚テンプレート」ボタン。
- **④ 分岐フローの可視化**：`components/BranchFlow.tsx`。**React Flowは使わず軽量SVGで自己完結**（依存追加なし）。
  設問=ノード、出題順=縦線、条件付き表示=右側の曲線矢印＋選択肢ラベル。右ペインのタブで表示。
- **⑤ 送信前の確認・修正画面**：`AnswerForm` に確認ステップ追加。最終設問の「回答内容を確認する」で全回答一覧を表示、
  各行「修正」で該当設問へジャンプ。`answerSummary()` で選択肢/グリッド等を可読テキスト化。
- **⑥ スマホ最適化**：選択肢を**行全体タップ可能なカード**化（選択ハイライト）。設問カードで**左右スワイプ移動**
  （onTouchStart/End、横60px超）。進捗ドットは到達済みのみクリックでジャンプ可。
- **⑦ オフライン対応・送信リトライ**：`public/sw.js`（Service Worker：アプリ殻キャッシュ＋Background Sync
  `kikitai-submit` で復帰時にクライアントへ再送依頼メッセージ）。送信は**サーバーアクション経由**のため実再送はクライアントが実施。
  オフライン/失敗時は payload を localStorage `kikitai-pending-<surveyId>` に退避＋バナー表示、`online`復帰や
  SWメッセージで自動再送、「今すぐ再送信」ボタンも提供。redirect(NEXT_REDIRECT)はそのまま伝播させ成功扱い。
- **⑧ マイクロフィードバック**：`app/globals.css` にアニメ追加（✓ポップ/スライドイン/保存点滅、reduced-motion対応）。
  「次へ」で✅ポップ、設問切替でスライドイン、自動保存時に「✓保存しました」を点滅表示。

## ★直近の変更（回答UI・Proプラン）
- 回答フォーム（`AnswerForm`）をスクロール式から**1問ずつ表示するステッパー**に変更。
  進捗インジケーター（問X/N・残り約Y分・ドット表示）と**途中保存**（localStorage
  `kikitai-draft-<surveyId>`）を実装。再訪時に「続きから再開／最初からやり直す」。送信成功で下書き削除。
  条件付き表示は visibleQuestions として動的に反映（非表示設問は飛ばす）。
- Proプラン：`profiles.plan`（free/pro、migration `20260610160000_profile_plan.sql`・sync済み）。
  プロフィール画面（`ProfileForm`内 `PlanManager`）で加入/解約トグル（デモは決済なし）。
- 統計解析モード（Pro限定）：結果画面にタブ追加（`?mode=stats`）。`lib/domain/statistics.ts`＋
  `ResultStats`で平均・中央値・標準偏差・最頻値などを表示。非Proはアップグレード誘導、
  ゲートはサーバー側（`profile.plan`）で判定。

## ★直近の変更（条件付き表示＝分岐）
- 設問に「表示条件」を追加。先行の選択式設問で特定の選択肢が選ばれた人だけに、その設問を表示。
- DB：`questions.condition jsonb`（migration `20260610150000_question_condition.sql`、sync済み）。
  形式 `{ sourceQuestionOrder:number, optionText:string }`。null は常に表示。
- 共通ロジック：`lib/domain/questions/visibility.ts` の `computeVisibleQuestionIds()` を
  クライアント(`AnswerForm`)とサーバー(`responseService.submitResponse`)の両方で使用し、
  表示・必須検証・保存対象を一致させる。**非表示設問は必須でも検証されず保存もされない**。
- 編集UI：`SurveyEditor` の `ConditionEditor`。条件元は「自分より前の選択式設問」のみ。
  editor内では source を `key` で参照し、保存時に `order_index` へ変換（`fromSurvey`で復元）。

## ★直近の変更（UI直感化）
- `components/QuestionTypePicker.tsx` を新設。作成画面の設問タイプ選択を、固有名詞だけの
  `<select>` から「プルタブ→開くと各タイプの完成イメージ図（SVG）ボタンが並ぶ」UIに置換
  （`SurveyEditor.tsx` で使用）。やさしい呼び名＋固有名詞を補足表示。
- `components/ResultChart.tsx` を新設。結果ページの選択式集計を横バーのみから
  「円グラフ（ドーナツ）＋凡例＋カラー棒グラフ」に変更（`results/page.tsx`）。
  複数選択は割合合計が100%にならないため棒グラフのみ・total=回答数で算出。
- 注意：eslintの `react-hooks/immutability` でレンダー中の変数再代入は不可。累積値は
  `slice().reduce()` で関数的に計算すること。

---

## 0. これは何のプロジェクト？

学生・研究者が互いにアンケートに回答し合うP2P型学術アンケート交換プラットフォーム
**「キキタイ」** のWebアプリ。現在は **Phase 1（デモ）** を実装済み。

- 作業フォルダ：`C:\大学\キキタイ\キキタイ\kikitai`
- 要件定義：親フォルダの `要件定義_v1.md`（デモ範囲＝正）／ `要件定義.md`（全体v2.0・将来機能）
- 開発ロードマップ：親フォルダの `開発ロードマップ.md`

## 1. 技術スタック

- Next.js **16**（App Router / TypeScript / Turbopack）※従来のNextと破壊的変更あり（後述）
- Tailwind CSS v4
- Supabase（PostgreSQL + Auth）
- Supabase CLI（`supabase` をdev依存に同梱、`npx supabase` で利用）

## 2. 環境・実行の前提（重要な落とし穴）

- **Node.js は PATH に通っていない**。実行時は毎回 PATH 追加が必要：
  - PowerShell: `$env:Path = "C:\Program Files\nodejs;" + $env:Path`
  - node: v24 / npm: v11
- **`.env.local` はUTF-8（BOMなし）**で保存すること。BOMが付くと先頭キーが壊れる。
- 開発サーバー：`npm run dev` → http://localhost:3000
- 型チェック：`npx tsc --noEmit` / ビルド：`npx next build`

### Next.js 16 の注意（AGENTS.md も参照）
- `cookies()` / `params` / `searchParams` は **非同期（Promise）**。`await` する。
- middleware は **`proxy.ts`** に改称（ルート直下）。本アプリの認証ガードは `proxy.ts` + `lib/supabase/proxy.ts`。
- コードを書く前に `node_modules/next/dist/docs/` の該当ガイドを読む方針。

## 3. アーキテクチャ（OOP）

UIから直接DBアクセスしない。層を分離：

```
app/                ページ＋ app/actions/（サーバーアクション）
components/          UI（クライアント/サーバー）
lib/
  supabase/         client(browser) / server / admin(service_role) / proxy
  services/         ビジネスロジック（AuthService 等）
  repositories/     DBアクセス（BaseRepository<T> を継承）
  domain/questions/ 設問タイプの多態（後述）
  types/
proxy.ts            認証ガード
supabase/migrations/  DBスキーマ（冪等マイグレーション）
scripts/supabase-sync.mjs  npm run sync の実体
```

- **設問タイプの多態**：`QuestionTypeDefinition`（抽象親）→ `ChoiceQuestionBase` → Single/Multiple/Scale、`TextQuestion`。`QuestionTypeRegistry` 経由でのみ利用。**新タイプ（順位付け・マトリクス等）はクラス追加＋レジストリ登録だけ**で済む設計（将来のポイント `pointCost` も保持）。
- **リポジトリ抽象基底**：`BaseRepository<T>`（共通CRUD）。

## 4. Supabase 運用

- スキーマは `supabase/migrations/` でバージョン管理（**冪等**＝再実行安全）。
- **手動でSQLをダッシュボードに貼らない**。`npm run sync` か `npm run db:push` で反映。
- `npm run sync` の中身（`scripts/supabase-sync.mjs`）：
  1. Management API で **メール確認OFF（mailer_autoconfirm=true）** を設定
  2. マイグレーション反映：**DBパスワードがあれば `supabase db push`**、
     **無ければ Management API の query エンドポイントでマイグレーションSQLを直接実行**
     （`SUPABASE_ACCESS_TOKEN` だけで反映可能。冪等前提のフォールバック）。
     → これにより `SUPABASE_DB_PASSWORD` 未設定でもスキーマ反映が自動化された。
- 必要な秘密情報（`.env.local`・git管理外）：
  - `SUPABASE_ACCESS_TOKEN`（https://supabase.com/dashboard/account/tokens）
  - `SUPABASE_DB_PASSWORD`（db push に必要。未設定だとpushはスキップ）
  - `SUPABASE_SERVICE_ROLE_KEY`（退会でauth.usersごと削除する場合）
- プロジェクト参照（ref）：`cpyhkdlbwsiiwwucpkji`

### メール確認は OFF 済み
新規登録の `Invalid login credentials` 問題は **メール確認をOFFにして解決済み**
（ダッシュボード＋ `npm run sync` の両方で適用）。登録すると即ログイン状態でホームに入る。
※ OFF前に作った「未確認アカウント」は Authentication → Users から削除して登録し直す。

## 5. Git / PR 状況

- リモート：`https://github.com/TakatoGokyu/softwareA-prac`（default: `main`、自分のWRITE権限あり）
- 作業ブランチ：`feature/phase1-demo`
- **PR #1**：Phase 1デモ → **マージ済み**
- **PR #2**：メール制限解消・CLI自動化・レビュー指摘修正 → **オープン中**
  - https://github.com/TakatoGokyu/softwareA-prac/pull/2
- **PR #3**（オープン中）：https://github.com/TakatoGokyu/softwareA-prac/pull/3
  作業ブランチ `feature/google-form-style-surveys`。
  アンケート作成・閲覧・集計を **Googleフォーム相当** に再現＋改良。
  - 設問タイプ追加：プルダウン / 段落（長文）/ 日付 / グリッド（選択式・チェックボックス）、
    スケールを **可変（min〜max＋両端ラベル）** に拡張。`text` は記述式（短文）に。
  - 設問の **必須(required)トグル**・**設問ごとの説明文**。
  - **セクション（ページ分割）＋進捗バー**。回答画面はページ送り＋ページ内必須バリデーション。
  - 作成画面：**ドラッグ&ドロップ並べ替え**・**設問複製**・セクション間移動。
  - 結果：グリッドのクロス集計表表示＋**CSVダウンロード**（`/surveys/[id]/results/export`）。
  - 設計：設問タイプの多態（QuestionTypeDefinition系）に新クラスを追加しレジストリ登録のみ。
    必須判定・config(scale/grid)・CSV整形(renderAnswerText)も各タイプ定義にカプセル化。
  - **要DB反映**：マイグレーション `supabase/migrations/20260610130000_google_form_features.sql`
    （questions に required/description/config/section_index、surveys に sections、answers に
    row_label を追加。冪等）。**`npm run sync`（または `npm run db:push`）で反映が必要**。
- コミット規約：末尾に `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`
- `.env.local` は絶対にコミットしない（gitignore済み）。

### チーム共有のされ方（CLAUDE.md / SOP / 秘密情報）

- **Git管理下のものは共有される**：`CLAUDE.md`・`AGENTS.md`・`引継ぎ.md`・
  `scripts/supabase-sync.mjs`・`supabase/migrations/` 等。Claude Code は作業フォルダの
  `CLAUDE.md` を自動で読むため、**相手がそのコミットを pull すれば相手側のClaudeも同じSOPに従う**。
  - ただし反映は **PRがmainにマージされ、相手が `git pull` した後**（または当該ブランチを
    `fetch`/`checkout` した後）。未マージのブランチ上の変更は相手にはまだ効かない。
- **共有されないもの（各自で用意が必要）**：
  - `.env.local`（`SUPABASE_ACCESS_TOKEN` 等の秘密情報）は gitignore 済み。
    相手が `npm run sync` でDB反映を行うには **相手自身のアクセストークンを各自の `.env.local`
    に設定**する必要がある（未設定だと sync はDB反映をスキップ）。
  - 個人設定（`~/.claude` のメモリ・キーバインド等）もリポジトリ外なので共有されない。
- **DBは共有の1つ**：スキーマ反映は誰か1人が `npm run sync` すれば全員に反映される。
  相手が sync を叩くのは「自分で新しいマイグレーションを追加したとき」だけでよい。

## 6. レビュー（修正ポイント）

`docs/demo_review_fix_points.md` にサブエージェント（Sonnet 4.6）のレビューあり。
- **高優先度6件＋軽微2件（FIX-01〜06,11,15）は対応済み**。
- 中・低（FIX-07〜10,12〜14,16）は**未対応・記録のみ**（必要に応じ着手）。

## 7. 未対応・TODO / 申し送り

- [x] ✅ **PR3のDB反映済み**：`npm run sync` で `20260610130000_google_form_features.sql`
      をリモートDBへ適用済み（Management API 経由。新カラム6件をライブDBで確認済み）。
- [ ] 🔴 **アクセストークンの再発行**：チャットに一度貼られたトークン（`sbp_2ac6...`）は漏洩扱い。Revoke して再発行し `.env.local` を差し替える（ユーザー作業）。
- [x] `SUPABASE_DB_PASSWORD` 未設定問題は解消（sync が Management API でSQLを直接反映する
      フォールバックを実装済み）。DBパスワードを設定すれば従来どおり `db push` も使える。
- [ ] レビュー中・低優先度（FIX-07〜10,12〜14,16）の対応可否を判断。
- [ ] 退会でauth.users削除を有効にするなら `SUPABASE_SERVICE_ROLE_KEY` を設定。

## 8. このファイルの運用ルール

- セッション開始時に**必ず読む**（CLAUDE.md から参照される）。
- 次のような変化があったら**その都度このファイルを更新**する：
  - 機能・設計の追加/変更、PRの作成・マージ、ブランチ変更
  - Supabase設定・環境変数・運用手順の変更
  - 未対応TODOの増減
- 更新時は冒頭の「最終更新」日付も直す。
